Code:
:- initialization(main). 
min(X, Y, X) :- X =< Y. 
min(X, Y, Y) :- X > Y. 
main :- 
min(3, 5, Min), 
write(Min), nl. 
 
Code: 
:- initialization(main). 
max(X, Y, X) :- X >= Y. 
max(X, Y, Y) :- X < Y. 
main :- 
max(3, 5, Max), 
write(Max), nl.
